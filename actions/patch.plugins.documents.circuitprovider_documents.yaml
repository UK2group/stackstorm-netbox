# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 3.7
description: "Patch a Circuit Provider Document object."
enabled: true
entry_point: run.py
name: patch.plugins.documents.circuitprovider_documents
parameters:
  endpoint_uri:
    default: "/plugins/documents/circuitprovider-documents/{{ id }}/"
    immutable: true
    type: string
  http_verb:
    default: patch
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  name:
    required: false
    type: string
    description: "(Optional) Specify a name to display for this document. If no name is specified, the filename will be used."
  document:
    required: false
    type: string
    description: "Document"
  external_url:
    required: false
    type: string
    description: "External url"
  document_type:
    required: false
    type: string
    description: "* `contract` - Contract
* `msa` - MSA
* `purchaseorder` - Purchase Order
* `quote` - Quote
* `other` - Other"
  provider:
    required: false
    type: integer
    description: "Provider"
  comments:
    required: false
    type: string
    description: "Comments"
  tags:
    required: false
    type: array
    description: "Array of tag strings"
  custom_fields:
    required: false
    type: object
    description: "Custom fields"
  id:
    required: true
    type: integer
    description: "ID of the object to patch."
runner_type: python-script
