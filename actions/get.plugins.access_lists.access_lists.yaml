# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 3.7
description: "Defines the view set for the django AccessList model &amp; associates it to a view."
enabled: true
entry_point: run.py
name: get.plugins.access_lists.access_lists
parameters:
  endpoint_uri:
    default: "/plugins/access-lists/access-lists/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  comments:
    required: false
    type: string
    description: "Comments"
  comments__ic:
    required: false
    type: string
    description: "Comments contains (case-insensitive)"
  comments__ie:
    required: false
    type: string
    description: "Comments exact match (case-insensitive)"
  comments__iew:
    required: false
    type: string
    description: "Comments ends with (case-insensitive)"
  comments__isw:
    required: false
    type: string
    description: "Comments starts with (case-sensitive)"
  comments__n:
    required: false
    type: string
    description: "Comments not equal to"
  comments__nic:
    required: false
    type: string
    description: "Comments does not contain (case-insensitive)"
  comments__nie:
    required: false
    type: string
    description: "Comments inverse exact match (case-insensitive)"
  comments__niew:
    required: false
    type: string
    description: "Comments does not end with (case-insensitive)"
  comments__nisw:
    required: false
    type: string
    description: "Comments does not start with (case-sensitive)"
  created:
    required: false
    type: array
    description: "Created"
  created__empty:
    required: false
    type: array
    description: "Created is empty/null (boolean)"
  created__gt:
    required: false
    type: array
    description: "Created greater than"
  created__gte:
    required: false
    type: array
    description: "Created greater than or equal to"
  created__lt:
    required: false
    type: array
    description: "Created less than"
  created__lte:
    required: false
    type: array
    description: "Created less than or equal to"
  created__n:
    required: false
    type: array
    description: "Created not equal to"
  created_by_request:
    required: false
    type: string
    description: "Created_by_request"
  default_action:
    required: false
    type: string
    description: "Default_action"
  default_action__n:
    required: false
    type: string
    description: "Default_action not equal to"
  device:
    required: false
    type: array
    description: "Device (name)"
  device__n:
    required: false
    type: array
    description: "Device not equal to"
  device_id:
    required: false
    type: array
    description: "Device (ID)"
  device_id__n:
    required: false
    type: array
    description: "Device_id not equal to"
  id:
    required: false
    type: array
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: array
    description: "Id greater than"
  id__gte:
    required: false
    type: array
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: array
    description: "Id less than"
  id__lte:
    required: false
    type: array
    description: "Id less than or equal to"
  id__n:
    required: false
    type: array
    description: "Id not equal to"
  last_updated:
    required: false
    type: array
    description: "Last_updated"
  last_updated__empty:
    required: false
    type: array
    description: "Last_updated is empty/null (boolean)"
  last_updated__gt:
    required: false
    type: array
    description: "Last_updated greater than"
  last_updated__gte:
    required: false
    type: array
    description: "Last_updated greater than or equal to"
  last_updated__lt:
    required: false
    type: array
    description: "Last_updated less than"
  last_updated__lte:
    required: false
    type: array
    description: "Last_updated less than or equal to"
  last_updated__n:
    required: false
    type: array
    description: "Last_updated not equal to"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  modified_by_request:
    required: false
    type: string
    description: "Modified_by_request"
  name:
    required: false
    type: array
    description: "Name"
  name__empty:
    required: false
    type: boolean
    description: "Name is empty/null (boolean)"
  name__ic:
    required: false
    type: array
    description: "Name contains (case-insensitive)"
  name__ie:
    required: false
    type: array
    description: "Name exact match (case-insensitive)"
  name__iew:
    required: false
    type: array
    description: "Name ends with (case-insensitive)"
  name__isw:
    required: false
    type: array
    description: "Name starts with (case-sensitive)"
  name__n:
    required: false
    type: array
    description: "Name not equal to"
  name__nic:
    required: false
    type: array
    description: "Name does not contain (case-insensitive)"
  name__nie:
    required: false
    type: array
    description: "Name inverse exact match (case-insensitive)"
  name__niew:
    required: false
    type: array
    description: "Name does not end with (case-insensitive)"
  name__nisw:
    required: false
    type: array
    description: "Name does not start with (case-sensitive)"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  q:
    required: false
    type: string
    description: "Search"
  tag:
    required: false
    type: array
    description: "Tag"
  tag__n:
    required: false
    type: array
    description: "Tag not equal to"
  type:
    required: false
    type: string
    description: "Type"
  type__n:
    required: false
    type: string
    description: "Type not equal to"
  updated_by_request:
    required: false
    type: string
    description: "Updated_by_request"
  virtual_chassis:
    required: false
    type: array
    description: "Virtual Chassis (name)"
  virtual_chassis__n:
    required: false
    type: array
    description: "Virtual_chassis not equal to"
  virtual_chassis_id:
    required: false
    type: array
    description: "Virtual Chassis (ID)"
  virtual_chassis_id__n:
    required: false
    type: array
    description: "Virtual_chassis_id not equal to"
  virtual_machine:
    required: false
    type: array
    description: "Virtual Machine (name)"
  virtual_machine__n:
    required: false
    type: array
    description: "Virtual_machine not equal to"
  virtual_machine_id:
    required: false
    type: array
    description: "Virtual machine (ID)"
  virtual_machine_id__n:
    required: false
    type: array
    description: "Virtual_machine_id not equal to"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
