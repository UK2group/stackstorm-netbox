# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 3.7
description: "Patch a DHCP Relay Service object."
enabled: true
entry_point: run.py
name: patch.plugins.switch_attributes.dhcp_relay_services
parameters:
  endpoint_uri:
    default: "/plugins/switch-attributes/dhcp-relay-services/{{ id }}/"
    immutable: true
    type: string
  http_verb:
    default: patch
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  device:
    required: false
    type: integer
    description: "Device to run DHCP Relay"
  vrf:
    required: false
    type: integer
    description: "Select the VRF for DHCP Relay to be started."
  servers:
    required: false
    type: array
    description: "Servers"
  interfaces:
    required: false
    type: array
    description: "Interfaces"
  id:
    required: true
    type: integer
    description: "ID of the object to patch."
runner_type: python-script
