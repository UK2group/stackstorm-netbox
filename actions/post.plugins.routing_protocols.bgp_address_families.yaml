# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 3.7
description: "Post a list of BGP Address Family objects."
enabled: true
entry_point: run.py
name: post.plugins.routing_protocols.bgp_address_families
parameters:
  endpoint_uri:
    default: "/plugins/routing-protocols/bgp-address-families/"
    immutable: true
    type: string
  http_verb:
    default: post
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  device:
    required: false
    type: integer
    description: "Device"
  vrf:
    required: false
    type: integer
    description: "Vrf"
  family:
    required: false
    type: string
    description: "* `ipv4-unicast` - IPv4 Unicast
* `ipv6-unicast` - IPv6 Unicast
* `l2vpn-evpn` - L2VPN EVPN"
  bgprouter:
    required: false
    type: integer
    description: "BGP Router"
  enable:
    required: false
    type: boolean
    description: "Enable the Address Family.

* `True` - Yes
* `False` - No"
  aggregate_routes:
    required: false
    type: array
    description: "Aggregate routes"
  aggregate_route_map:
    required: false
    type: integer
    description: "Route Map"
  networks:
    required: false
    type: array
    description: "Network Statements"
  network_route_map:
    required: false
    type: integer
    description: "Route Map"
  export_to_evpn:
    required: false
    type: boolean
    description: "EVPN Export"
runner_type: python-script
